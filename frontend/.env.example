# Build-time only (Vite inlines import.meta.env.* at build time) - set these
# as the Render Static Site's build environment variables in production.
VITE_API_GATEWAY_URL=https://healthsync-api-gateway.onrender.com
VITE_NOTIFICATION_SERVICE_URL=https://healthsync-notification-service.onrender.com
